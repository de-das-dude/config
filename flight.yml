spring:
  datasource:
    username: root
    password: admin
    url: jdbc:mysql://localhost:3306/flight
  jpa:
      show-sql: true
      hibernate:
          ddl-auto: update
        
      properties:
        hibernate:
            dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 8200
